import "./DimmedLayer.css";

function DimmedLayer({ width, height }) {
  return (
    <div className="dimmed_layer" style={{ width, height }}>
      <div className="dimmed_layer_background">
        
      </div>
    </div>
  );
}

export default DimmedLayer;
